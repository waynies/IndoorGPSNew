package com.IndoorGPS;

// TODO: make this compatible

public class ApplicationConfiguration
{
	public static final String SAMPLE_COUNT = "3";
	public static final String MAX_NUM_AP = "100";
	
	/*private static boolean DEBUG = true;
    private static int DEBUG_trackcount = 0;
    private static int DEBUG_loccount = 0;


    //WIFI Adaptor Settings
    private static Boolean adapterFound;
    private static Adapter networkAdap = null;

    // General Settings
    private static String building, floor;

    private static DataTable apTable = null;  

    private static string mapImageFile = null;
    private static Bitmap mapImage = null;       
    private static Graphics mapImageGraphics = null;

    private static int ptr_x, ptr_y;

    private static Point StateLoc;

    private static LocAlg chosenLocAlg;

    //Tracking
    private static int trackingChoice = 0;
    private static double trackWeight = 0;
    private static int trackUpdateInterval = 1;
    private static double initVx = 0;
    private static double initVy = 0;
    private static bool isTrackingOn = false;
    private static Thread trackingThread;

    private delegate void locUpdateTrackDelegate(Point meas, Point est, double traveldistance, string numst);
    private delegate void locUpdateSmoothDelegate();
    private delegate void progBarValueDelegate(int value);
    private delegate void progBarVisibleDelegate(bool vis);

    private static bool collectRSSOnly = false;
    private static int tracecount = 0;
    private static int stepcount = 0;

    private static bool refreshScreen = false;

    private static bool navigationSelected = false;

    private static string traceFile = null;


    // Navigation Settings
    private static bool isNavigationRunning = false;
    
    private Thread navigationThread;
    private static int routeCount = 1;

    private delegate void locUpdateNavigDelegate(int x, int y, string numst);
    private delegate void pathNavigDelegate(Point[] pts);
    private delegate void displaytxtDelegate(string msg, bool append);
    private delegate void buttonDelegate();


    private static bool firstloc4navig = false;

    private static List<Point> mapSelectedPoints = new List<Point>();


    private static bool navigStartPtKnown = false;        
    private static string userStartingPt = null;

    //for subject testing
    private static bool subjectTestingMode = false;
    private static string testID = null;
    private static string subjectID = null;


    //for hardward keys
    private HardwareButton hwb1, hwb4, hwb2, hwb3, hwb5, hwb6;

    //for repeat voice commands
    private static bool isRepeatVoicePlaying = false;
    private static bool isNavigUpdateVoicePlaying = false;
    private static bool isVoiceCopying = false;
    private static List<VoiceInstr> RepeatVoiceBuffer = new List<VoiceInstr>();
    private static List<string> RepeatVoiceAdditionalBuffer = new List<string>();
    private static DateTime repeatCmdCopyTime;

    //for compass
    private static bool isDCworking = false;
    private static int headingDC = -1;
    private static bool isDCon = false;
    private uint periodDC;
    private S3DOrientation.Position DCreading = new S3DOrientation.Position();


    //for accelerometer
    private static bool isACCworking = false;
    private static bool isACCon = false;
    private static List<Accelerometer.Vector> accelerationTimeSamples = new List<Accelerometer.Vector>();
    private uint periodACC;*/
}
